export type WorkspaceRole = "owner" | "editor" | "viewer";

export type NoteType = "quick" | "journal" | "markdown" | "outline";

export type TaskKind = "todo" | "learning" | "project" | "problem" | "bug";

export type TaskStatus = "todo" | "in_progress" | "blocked" | "done" | "cancelled";

export type ReflectionType = "cognitive" | "error_review" | "project_review" | "weekly";

export type PhaseAvailability = "available" | "preview" | "planned";
